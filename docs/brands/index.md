---
title: "브랜드 몰 관리 > 브랜드 리스트"
slug: /brands
sidebar_position: 16
---

> _최종 업데이트: 2026-09-03_

## 페이지 설명

- 브랜드 운영 대상 목록 확인 화면 — 새 브랜드 등록·담당자 지정 가능
- 일부 브랜드는 이 페이지에서 바로 `가맹본부`, `매장` 화면으로 이동 가능

## 기능 요약

| 기능 | 내용 |
| --- | --- |
| [브랜드 등록](#브랜드-등록) | 브랜드 정보 입력 후 신규 생성 |
| [담당 MD 지정 또는 변경](#담당-md-지정-또는-변경) | 브랜드별 담당 관리자 연결 |
| [브랜드 화면 바로가기](#브랜드-화면-바로가기) | `가맹본부`·`매장` 화면 새 탭 이동 |
| [전용상품 재발주 제한 설정](#전용상품-재발주-제한-설정) | 브랜드 상세에서 전용상품 재발주 제한 토글 관리 |

## 브랜드 등록

1. 상단 `브랜드 추가` 클릭
2. 열린 `브랜드 추가` 화면에서 필요한 브랜드 정보 입력
3. 저장 완료

- **완료**: `브랜드 생성 성공` 안내 후 생성된 브랜드의 상세 화면으로 이동
- **제한**: 필수 정보가 비어 있으면 저장 불가
- **⚠️ 유의**: 브랜드 상세에서 이어서 입력할 정보가 많음 — 생성 직후 상세 화면 이동 여부 확인 필요

## 담당 MD 지정 또는 변경

1. 목록의 담당 MD 영역에서 `지정` 또는 `변경` 클릭
2. `관리자 선택(MD)` 창에서 연결할 관리자 선택
3. 저장 완료

- **완료**: `브랜드몰 확인용 관리자 설정이 적용 되었습니다.` 표시
- **⚠️ 유의**: 담당자 없는 브랜드에는 `지정`, 이미 연결된 브랜드에는 `변경` 표시

## 브랜드 화면 바로가기

1. 이동할 브랜드의 `가맹본부` 또는 `매장` 버튼 클릭
2. 새 탭에서 열린 화면으로 이동해 필요한 내용 확인

- **완료**: 클릭한 종류에 맞는 화면이 새 탭으로 열림
- **⚠️ 유의**: 해당 브랜드가 바로가기 대상이 아니면 버튼 대신 `-` 표시

## 전용상품 재발주 제한 설정

1. 브랜드 상세 화면(수정 모드) 기본 정보에서 "전용상품 재발주 제한" 토글 변경
2. 활성화된 "전용상품 재발주 제한 변경 사유" 입력
3. 저장 진행

- **완료**: 설정 반영 → 제한 브랜드의 전용상품 발주 시 발주서 등록 확인 모달에서 재발주 제한 확인 절차 진행됨
- **제한**: 토글 값을 저장값과 다르게 변경한 경우에만 사유 입력란 활성화되며 이때 필수 입력
- **⚠️ 유의**: 토글을 다시 원래 값으로 되돌리면 기존 저장 사유 복원, 변경하면 사유 입력값 자동 초기화 / 신규 브랜드 등록 화면에는 미노출 / 재발주 제한 설정·해제 이력은 브랜드 이력에 자동 등록되며 삭제 불가 / 브랜드 상세의 기타 비용 내역 "상세내용"은 최대 1000자 입력 가능(글자수 카운터 표시, 초과 시 "최대 1000자 이하로 입력해주세요.")
